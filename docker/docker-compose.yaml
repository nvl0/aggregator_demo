services:
  db:
    image: postgres:16.4-alpine
    environment:
      POSTGRES_USER: aggregator
      POSTGRES_PASSWORD: aggregator
      POSTGRES_DB: aggregator
      PGDATA: /data
      TZ: 'GMT+5'
    volumes:
      - ./pg/data:/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U aggregator" ]
      interval: 5s
      timeout: 10s
      retries: 5
    ports:
      - "5432:5432"
  migrate:
    image: migrate/migrate
    command:
      [
        "-path",
        "/migration",
        "-database",
        "postgres://aggregator:aggregator@db:5432/aggregator?sslmode=disable",
        "up",
        "1"
      ]
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ../docker/migrate:/migration
  aggregator:
    image: ${NAME}:${VERSION}
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
      args:
        VERSION: ${VERSION}
        NAME: ${NAME}
    environment:
      DEBUG: 'true'
      MEASURE: 'enable'
      POSTGRES_IP_PORT: db
      VERSION: ${VERSION}
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ../src/log:/src/log
      - ../src/config/conf.yaml:/src/conf.yaml
      - ../src/flow:/src/flow
      - ../src/subnet-disabled:/src/subnet-disabled
